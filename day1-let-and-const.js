function main() {
  let r = readLine();
  // Print the area of the circle:
  console.log(Math.PI * parseFloat(r ** 2));
  // Print the perimeter of the circle:
  console.log(2 * Math.PI * parseFloat(r));
}
